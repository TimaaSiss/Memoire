import { EtablissementModel } from './etablissement.model';

describe('EtablissementModel', () => {
  it('should create an instance', () => {
    expect(new EtablissementModel()).toBeTruthy();
  });
});
